// object calling in variables
let person = {
    name :"rahul",
    id :77,
    isemployee : true ,
};
person.bloodgroup = "b+";
person. isemployee = false;
delete person.id;

console.log(person);
console.log(person.id);

//object.keys
console.log(Object.keys(person));

